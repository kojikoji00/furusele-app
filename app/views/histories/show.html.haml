
= render 'commons/header'

%h3.deduction
  = "控除上限額の目安：#{@deduction.deduction.to_s(:delimited)}円"
-# .select-catgory
-#   = "あなたの選んだカテゴリーは#{Category.find(id: @category.id).name}です"

.result
  .result-title
    %h3 結果一覧
    %p ※商品情報は「さとふる」より引用しています。
    %p 　商品を購入する際は商品のリンク先よりご注文ください。
  %ul
    - @history_details.each do |item|
      %li
        = link_to item.link do
          .result-image
            = image_tag item.picture_image_path
          .result-name
            %h4
              = item.name
          .result-city
            %p
              = item.city
          .result-price
            %p
              -# = item.price
              = "#{item.price}円"

          .result-description
            %p
              = item.description
          .result-review
            = image_tag item.review_image_path


.link
  %h4 他にも気になる商品があればリンク先から検索ください。
  %ul
    %li
      = link_to 'https://www.satofull.jp/' do
        .link-satofull
          %i.fas.fa-angle-right　さとふる
    %li
      = link_to 'https://event.rakuten.co.jp/furusato/' do
        .link-rakuten
          %i.fas.fa-angle-right　楽天ふるさと納税

= render 'commons/footer'
